{
  "name": "puppeteer-cluster-poc",
  "version": "1.0.0",
  "description": "",
  "main": "cluster.ts",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "start": "node dist/cluster.js",
    "dev": "concurrently \"npx tsc --watch\" \"nodemon -q dist/cluster.js\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tsconfig/node16": "^1.0.2",
    "@types/node": "^17.0.31",
    "concurrently": "^7.1.0",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.4"
  },
  "dependencies": {
    "puppeteer": "^13.7.0",
    "puppeteer-cluster": "^0.23.0"
  }
}
